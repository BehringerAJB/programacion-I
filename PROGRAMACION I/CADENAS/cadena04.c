#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main() {
    char str1[] = "Hola";
    
    int c;
    // Convertir cadenas a minúsculas antes de comparar
    for (int i = 0; str1[i]; i++) 
    {
        if(str[i]>='a' && str[i]<='z')
            str[i]=str[i]-32
    }
    printf("%s",str[i]);
    return 0;
}
